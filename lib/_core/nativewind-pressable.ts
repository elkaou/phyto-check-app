import { Pressable } from 'react-native';
import type { PressableProps } from 'react-native';

const NativeWindPressable = (props: PressableProps) => {
  return Pressable(props);
};

export { NativeWindPressable };
